pre-commit:
  commands:
    biome:
      glob: "*.{js,ts,jsx,tsx,json}"
      run: bunx biome format --write {staged_files} && git add {staged_files}
    cargo-fmt:
      glob: "*.rs"
      run: cargo fmt --manifest-path proxy-gateway/Cargo.toml && git add {staged_files}
