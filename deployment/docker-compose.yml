services:
  proxy-gateway:
    image: ghcr.io/traumwohnung/proxy-gateway:main
    # Or build locally:
    # build:
    #   context: ..
    #   dockerfile: Dockerfile
    ports:
      - "8100:8100"
    volumes:
      - ./config:/data/config:ro
    environment:
      - LOG_LEVEL=info
    restart: unless-stopped
